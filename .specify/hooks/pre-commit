#!/bin/sh
# Spec lint, spec→implementation→test traceability, and the full test suite with coverage.
# Commit-message rules (IDs, bug-fix discipline) run in the commit-msg hook.
npm run lint-specs && npm run verify-specs && npm run coverage
